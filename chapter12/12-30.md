**English:** A. List only one deadlock scenario:
Thread 1: holds `a`, needs `b`
Thread 2: holds `c`, needs `b`
Thread 3: holds `b`, needs `a`

**简体中文：** A.只列出一种死锁情况：<br/>
线程1：持有a,需要b<br/>
线程2：持有c,需要b<br/>
线程3：持有b,需要a<br/>


**English:** B. The problem states that `a < b < c` — for example `a = 1`, `b = 2`, `c = 3` — so the locking order should be `abc`. Since `a` is larger and may experience lock contention, request `a`'s lock first, and then `b` and `c` won't encounter lock contention; therefore threads 2 and 3 are unreasonable.

**简体中文：** B.题目是说a<b<c,比如说a=1,b=2,c=3,那么加锁顺序应该是abc，因为a更大可能会出现锁争用，先请求a的锁，bc就不用发生锁争用了，所以线程2和3不合理 <br/>


**English:** C. Lock first and release later — for example, always keep the order `P(a) P(b) P(c) V(c) V(b) V(a)`.

**简体中文：** C.先加锁后释放，比如都保持P(a)P(b)P(c)V(c)V(b)V(a)的顺序<br/>
