int x = random();  
int y = random();  
int z = random();  

double dx = (double)x;  
double dy = (double)y;  
double dz = (double)z;  

**English:** Converting int to float: since float has only 23 fraction bits, it can represent only 23 bits exactly; values exceeding 23 bits are rounded. For example, `int x = 16777217; float y = (float)x = 16777216.0000`.

**简体中文：** > int转换成float，因为float小数位只有23位，所以只能精确表示23位，超过23位就会舍入。
>
> 例如：int x = 16777217; float y = (float)x = 16777216.0000

**English:** A. `(float) x == (float) dx` — True, because `dx` has no loss of precision.

**简体中文：** A.(float) x == (float) dx  
 对，因为dx没有精度丢失  

**English:** B. `dx - dy == (double) x - y` — False; if `x - y` overflows (e.g. `x = TMax`, `y = -1`), the two sides are not equal.

**简体中文：** B.dx-dy == (double) x - y  
 错，x - y如果溢出的话,x=TMax y=-1，就不相等了  

**English:** C. `(dx + dy) + dz == dx + (dy + dz)` — False; `(3.14 + 1e55) - 1e55 = 0`, but `3.14 + (1e55 - 1e55) = 3.14`.

**简体中文：** C.(dx+dy)+dz == dx+(dy+dz)  
 错，(3.14+1e55)-1e55=0,但是3.14+（1e55-1e55）=3.14  

**English:** D. `(dx*dy)*dz == dx*(dy*dz)` — False; `(1e40 * 1e40) * 1e-40 = +∞`, but `1e40 * (1e40 * 1e-40) = 1e40`.

**简体中文：** D.(dx*dy)*dz == dx*(dy*dz)  
 错，(1e40*1e40)*1e-40=+∞，但是1e40*（1e40*1e-40）等于1e40  

**English:** E. `dx/dx == dz/dz` — False; `0/0.0` is NaN, while `(nonzero)/0.0` is ±∞.

**简体中文：** E.dx/dx == dz/dz  
 错，0/0.0为NaN，(非0)/0.0为正负inf  
