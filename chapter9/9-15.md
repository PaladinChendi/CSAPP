**English:** The table below shows, for each request, the block size in decimal bytes and the block header in hexadecimal.

**简体中文：** 下表列出每个请求的块大小（十进制字节）和块头部（十六进制）。

| Request / 请求 | Block size (decimal bytes) / 块大小（十进制字节） | Block header (hex) / 块头部（十六进制） |
| ---------- | -------------------- | ------------------ |
| malloc(3)  | 8 Bytes / 8Bytes               | 0x00000009         |
| malloc(11) | 16 Bytes / 16Bytes              | 0x00000011         |
| malloc(20) | 24 Bytes / 24Bytes              | 0x00000019         |
| malloc(21) | 32 Bytes / 32Bytes              | 0x00000021         |


**English:** The point to note is double-word alignment: while ensuring the block size is a multiple of 8 bytes, you must also account for the 4 bytes occupied by the block header.

**简体中文：** 注意的地方就是双字对齐，考虑块大小是8字节的倍数的同时也要考虑块头部要占4字节
