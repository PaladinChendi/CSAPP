**English:** This problem took quite a bit of effort. At first I kept adding whatever came to mind and then fixing bugs found in testing; I realized this was very inefficient and I always missed cases. So I searched online for others' solution approaches, made a table covering all cases, and got it right on the first analysis-and-modify pass. I have to admire the ruthlessness of systematic thinking. The final answer is as follows:<br/>
<br/>

**English:** Referring to Figure 4-67 in the book, all the cases are listed as follows:<br/>
There are three basic data-hazard cases (execute/use, memory/use, write-back/use), and four control-hazard cases: JXX has only one, and ret has three.

**简体中文：** 这道题颇费了一番功夫，一开始是先把想到的加上去，然后测试发现bug再修改，发现这样效率很低，总是有漏调的，然后在网上搜了下大家的解题思路，列了一个全部情况的表格，分析修改一次通过，不得不赞叹系统思维的毒辣，最终答案如下：<br/>
<br/>
参照书上 图 4-67 列出所有的情况如下：<br/>
数据冒险有三种基本情况（执行/使用、访存/使用、写回/使用），控制冒险四种情况：JXX 只有一种，ret 有三种

**English:** The table below summarizes, for each pipeline register (W/M/E/D), what happens under each of the seven combined-hazard cases (①–⑦).

**简体中文：** 下表总结了每个流水线寄存器（W/M/E/D）在七种组合冒险情况（①–⑦）下的行为。

| 寄存器 / Register | ①执行/使用 Execute/Use | ②访存/使用 Memory/Use | ③写回/使用 Write-back/Use | ④JXX | ⑤ret | ⑥ret | ⑦ret |
| :----- | :--------- | :--------- | :--------- | :--- | :--- | :--- | :--- |
| **W**  | ——         | ——         | 写回 / write-back | ——   | ——   | ——   | ——   |
| **M**  | ——         | 访存 / memory | ——         | ——   | ——   | ——   | ret  |
| **E**  | 执行 / execute | ——     | ——         | JXX  | ——   | ret  | 气泡 / bubble |
| **D**  | 使用 / use | 使用 / use | 使用 / use | ——   | ret  | 气泡 / bubble | 气泡 / bubble |

**English:** Combination analysis:

**简体中文：** 组合分析：

**English:** ①② is an ordinary data hazard: in the next clock cycle F and D stall, E is injected with a bubble, and this continues until the W stage completes.

**简体中文：** ①②属于普通的数据冒险，下一个时钟周期F和D暂停，E加入气泡，直到W阶段完成

**English:** ①③ is the same as ①②.

**简体中文：** ①③同①②

**English:** ①⑤ is similar to combination B in the book: if D is injected with a bubble in the next clock cycle, the original instruction is cancelled, so it should be a stall.

**简体中文：** ①⑤和书上的组合B的情况相似，下一个时钟周期D如果加入气泡就把本来的指令取消了，所以应该是暂停

**English:** ②③ is a load/use hazard, the same as ①②.

**简体中文：** ②③就是加载/使用冒险，同①②

**English:** ②④ is similar to combination A in the book: in the next clock cycle bubbles are injected into D and E, and F stalls, but F selects the correct address, so the result is unaffected.

**简体中文：** ②④和书上组合A的情况相似，下一个时钟周期会在D和E中加入气泡，F会暂停，但是F会选择正确的地址，所以不会影响结果

**English:** ②⑤ is the same as ①⑤.

**简体中文：** ②⑤同①⑤

**English:** ③④ is the same as ②④.

**简体中文：** ③④同②④

**English:** ③⑤ is the same as ①⑤.

**简体中文：** ③⑤同①⑤

**English:** ④⑤ is exactly combination A in the book.

**简体中文：** ④⑤就是书上的组合A

**English:** ①②③ is the same as ①②.

**简体中文：** ①②③同①②

**English:** ②③④ is the same as ②④.

**简体中文：** ②③④同②④

**English:** ②③⑤ is the same as ①⑤.

**简体中文：** ②③⑤同①⑤

**English:** ③④⑤ is the same as ②④.

**简体中文：** ③④⑤同②④

**English:** ①②③⑤ is the same as ①⑤.

**简体中文：** ①②③⑤同①⑤

**English:** Overall, the cases requiring special handling are ①⑤, ②⑤, ③⑤, ②③⑤, and ①②③⑤ — these are all variants of combination B in the book, so the modifications are as follows:

**简体中文：** 综合来看，需要特殊处理的情况就是①⑤、②⑤、③⑤、②③⑤、①②③⑤，这其实都是书上组合B的变种，所以修改如下：

```
################ Pipeline Register Control #########################

# Should I stall or inject a bubble into Pipeline Register F?
# At most one of these can be true.
bool F_bubble = 0;
bool F_stall =
	# Modify the following to stall the update of pipeline register F
	0 ||
	(d_srcA != RNONE && 
	 d_srcA in { e_dstE, E_dstM, M_dstM, M_dstE, W_dstM, W_dstE }) ||
	(d_srcB != RNONE && 
	 d_srcB in { e_dstE, E_dstM, M_dstM, M_dstE, W_dstM, W_dstE }) ||
	# Stalling at fetch while ret passes through pipeline
	IRET in { D_icode, E_icode, M_icode };

# Should I stall or inject a bubble into Pipeline Register D?
# At most one of these can be true.
bool D_stall = 
	# Modify the following to stall the instruction in decode
	0 ||
	(d_srcA != RNONE && 
	 d_srcA in { e_dstE, E_dstM, M_dstM, M_dstE, W_dstM, W_dstE }) ||
	(d_srcB != RNONE && 
	 d_srcB in { e_dstE, E_dstM, M_dstM, M_dstE, W_dstM, W_dstE });

bool D_bubble =
	# Mispredicted branch
	(E_icode == IJXX && !e_Cnd) ||
	# Stalling at fetch while ret passes through pipeline
	# !(E_icode in { IMRMOVQ, IPOPQ } && E_dstM in { d_srcA, d_srcB }) &&
	!((d_srcA != RNONE && 
	 d_srcA in { e_dstE, E_dstM, M_dstM, M_dstE, W_dstM, W_dstE }) ||
	(d_srcB != RNONE && 
	 d_srcB in { e_dstE, E_dstM, M_dstM, M_dstE, W_dstM, W_dstE })) &&
	# but not condition for a generate/use hazard
	!0 &&
	  IRET in { D_icode, E_icode, M_icode };

# Should I stall or inject a bubble into Pipeline Register E?
# At most one of these can be true.
bool E_stall = 0;
bool E_bubble =
	# Mispredicted branch
	(E_icode == IJXX && !e_Cnd) ||
	# Modify the following to inject bubble into the execute stage
	0 ||
	(d_srcA != RNONE && 
	 d_srcA in { e_dstE, E_dstM, M_dstM, M_dstE, W_dstM, W_dstE }) ||
	(d_srcB != RNONE && 
	 d_srcB in { e_dstE, E_dstM, M_dstM, M_dstE, W_dstM, W_dstE }) ||
	(D_icode == IRET && RRSP in { e_dstE, E_dstM, M_dstM, M_dstE, W_dstM, W_dstE });

# Should I stall or inject a bubble into Pipeline Register M?
# At most one of these can be true.
bool M_stall = 0;
# Start injecting bubbles as soon as exception passes through memory stage
bool M_bubble = m_stat in { SADR, SINS, SHLT } || W_stat in { SADR, SINS, SHLT };

# Should I stall or inject a bubble into Pipeline Register W?
bool W_stall = W_stat in { SADR, SINS, SHLT };
bool W_bubble = 0;
```

