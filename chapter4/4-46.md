**English:** The test code for problem 4.7 is

**简体中文：** 4.7的测试代码是
```assembly
pushtest:
mov %rsp,%rax
pushq %rsp
popq %rdx
subq %rdx,%rax #return 0 or 8
```

**English:** In this problem the code for `popq REG` is

**简体中文：** 本题中popq REG的代码是
```assembly
movq (%rsp),REG
addq $8,%rsp
```

**English:** A. If we replace `popq` with the `popq REG` from this problem, we can see that when `REG=%rsp`, the top-of-stack data is first written into `REG`, and then `%rsp` is incremented by 8, so the test code for 4.7 returns 8.<br\>

**简体中文：** A.如果我们把popq替换成本题中的popq REG时，可以看到当REG=%rsp时，会先栈顶数据写入REG中，然后把%rsp加上8，4.7的测试代码返回8<br\>
B.```assembly
addq $8,%rsp
movq -8(%rsp),REG
```