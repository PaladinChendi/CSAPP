**English:** The cache is 2048 B, direct-mapped with 1 line per set, a block size of 32 B, 5 block-offset bits, 64 cache sets, and a 6-bit set index.

**简体中文：** cache大小2048B，直接映射每组1行，块大小为32B，块偏移为5位，缓存组数64，组索引为6位
```c
for(i = 0; i < 16; i++) {
    for(j = 0; j < 16; j++) {
        square[i][j].y = 1;
    }
}
for(i = 0; i < 16; i++) {
    for(j = 0; j < 16; j++) {
        square[i][j].c = 0;
        square[i][j].m = 0;
        square[i][j].k = 0;
    }
}
```
**English:** A. The total number of writes is 1024.
B. Since the cache can hold only 1/2 of the array, the first part is loaded into the cache and then replaced by the second part. Since each block can cache two array elements and each element has four values, we analyze the two parts separately: the first part writes 256 times with a 1/2 miss rate, giving 128 misses; the second part writes 768 times with a 1/6 miss rate, giving 128 misses. The total is 256 misses.
C. The miss rate is 25%.

**简体中文：** A.写总数是1024 <br/>
B.因为只能保存数组的1/2，所以第一部分加载到cache后，又会被第二部分替换 <br/>
因为每个块能缓存两个数组元素，每个元素四个值，所以我们分开来看： <br/>
第一部分写256次，有一半未命中，未命中128次 <br/>
第二部分写768次，有1/6未命中，未命中128次 <br/>
最终是256次未命中<br/>
C.不命中率25% <br/>
